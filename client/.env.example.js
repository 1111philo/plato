// Local development overrides — copy this to .env.js and fill in your values.
// .env.js is gitignored and will never be committed.
// On app load, these values seed storage if not already set.

export const ENV = {
  apiKey: 'YOUR_ANTHROPIC_API_KEY_HERE',
  name: 'Your Name'
};
